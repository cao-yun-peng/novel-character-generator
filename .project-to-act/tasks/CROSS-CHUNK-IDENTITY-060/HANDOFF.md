# CROSS-CHUNK-IDENTITY-060

已完成 Stage 5 内的跨 Chunk 身份纵向切片；Stage revision 保持 2，Stage 5 仍为 `in_progress`。已交付完整 local/promoted 人物节点目录、有界候选、M3 最小模型边界、严格/纯空白等价身份证据 Grounding、cannot-link、冲突保留、全局人物注册表，以及可恢复的 DeepSeek 批处理入口。

《斗破苍穹》既有产物已在零 Provider 调用下生成 23 个局部人物节点、1 条共享文档事实确定性边和 19 个待执行 M3 候选任务；每节点最多 2 个任务，包含 1 个零外貌事实 exact 节点。109 项自动测试和 Draft 2020-12 Schema/实例校验通过。

本任务未运行真实 DeepSeek 身份判断，也未完成人工身份精度 Gate。下一步应使用 `run-deepseek-document-identity` 执行 19 个任务，人工复核 same/different/uncertain、`萧薰儿/萧熏儿/熏儿` 合并、同名防误合并和泛称隔离，再决定是否进入 Stage 6。
