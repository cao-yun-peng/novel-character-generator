# E-20260828-CLEANUP-V2-016

- 时间：2026-08-28（Asia/Shanghai）
- 确认来源：用户明确授权永久删除，不保留运行或回滚能力。
- 代码基线：Git HEAD `3e07c9f`；工作树未提交。
- 保留范围：V2 总契约、M1/N2/M2 源码与测试、M3–M5 设计 Prompt、评测数据和四份真实小说样本。
- 删除范围：无关源代码、迁移、旧测试与文档、数据库、向量数据、图像工件、诊断目录和工具缓存。
- 依赖结果：运行依赖只剩 `httpx` 与 `pydantic`；`uv.lock` 已刷新。`pyproject.toml` SHA-256 `BEC99BCD97444807371DC844B7CD53318ECD71A69707FF7A1E78D2B659E447EB`，`uv.lock` SHA-256 `CC934147FA27F947C5DF9A58580D4D36E587B95DE525899854750F68B981CF1C`。
- 验证：`pytest -p no:cacheprovider`，退出 0，59 passed；`ruff check src tests scripts`，退出 0；`mypy`，退出 0，29 source files；三个 CLI `--help` 均退出 0；JSON 解析、`git diff --check`、项目账本 `--validate` 均退出 0。
- 规模：剩余 29 个 source 文件、19 个 test/data 文件、10 个 docs 文件、5 个任务目录；Git diff 为 656 files changed、约 271,949 行删除。
- 已知本机残留：`data/test-tmp` 约 585.9 MB/928 文件受 Windows ACL 拒绝，提权和 ACL reset 仍失败；该目录已忽略，原 160 个已跟踪临时文件均显示为删除，不影响验证。
- 有效期：代码、依赖或测试数据再次变化前有效。
