# M1 Prompt v2.9 泛化修正交接

## 已完成

- 以通用规则强化重复描述唯一定位和同载体不同视觉 cue 独立覆盖。
- 已同步运行时版本、评测模型版本、两套 Dataset 的 Prompt 元数据、测试和项目文档。
- 006、009 按用户复审结论不作为待修问题；Dataset 金标、Rubric 和 Source Match Policy 未改变。

## 未验证

- 没有调用 Provider，不能宣称 005 已修复，也不能从 v2.8 outputs 推断 v2.9 效果。
- 下一次需要用户再次确认外发授权后，使用 v2.9 运行短集 16 条和真实集 10 条。

## 重点观察

- 重复描述：模型是否保留足够的量词/角色/位置上下文使 quote 唯一。
- 同载体不同 cue：模型是否分别召回先前物理外观和后续状态/情绪谓词，且不产生纯动作冗余候选。
