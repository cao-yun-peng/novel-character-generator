# E-20260828-PIPELINE-V2-M1-013

## 结论

M1 局部观察发现的 Stage 5 shadow/offline 纵向切片工程 Gate 通过；模型效果 Gate 尚未开始，等待用户审核 15-case 独立测试集。未修改 Worker 默认路由、数据库或 active Observation，未发起真实 Provider 调用。

## 已实现

- `LocalObservationDiscoveryInput/Result` v1.1 严格 DTO；
- 局部 entity/fact/signal/unresolved ID 与引用图校验；
- 当前 Chunk 原文引文、allowed family、未使用 entity、重复事实、fact/unresolved 排他双写失败关闭；
- 含 contract/prompt/model/data/input/output 指纹、usage 和空结果审计的不可变 shadow artifact；
- OpenAI-compatible M1 请求、Schema、有限重试、raw/usage metadata；
- 15-case `m1-local-observation-v1-draft1` 测试集、三态评分器与离线 CLI；
- raw fact recall、supported precision、quote fidelity、epistemic accuracy、temporal recall 与逐 case reason。

## 冗余整理与删除审计

删除：

1. `docs/prompts/semantic-pipeline-v2/01-local-observation-discovery.system.md`。运行时 package Prompt 成为唯一内容来源，设计文档改为链接它。
2. R1/R2 各自重复的 HTTP client、deadline、重试、Provider response metadata/usage 组装循环，以及只为这些循环保存的连接配置字段。R1、R2、M1 统一复用 `OpenAICompatibleStructuredClient`。

保留：

- V1 extraction DTO/Prompt/adapter/Worker：当前生产链仍直接引用，M2 尚未实现，删除会造成能力缺口；
- `visual_extraction_seed_v0/v1`、`r1_quality_regression_v1` 及旧评分测试：仍被历史复现、当前 A/B、回滚和文档直接引用；
- V1 Provider 业务适配层：仍拥有 V1 Prompt、Schema 与 item-limit 校验，不与公共传输层重复。

## 验证证据

- `pytest` M1/R1 Provider 定向：40 passed；
- `pytest` R1/R2 Provider 与恢复边界：30 passed；
- 完整 `pytest -p no:cacheprovider`：251 passed，1 个第三方 Starlette/httpx deprecation warning；
- `ruff check src tests scripts`：通过；
- `mypy src/novel_character_generator`：125 source files 通过；
- `git diff --check`：通过，仅有工作区既有 LF/CRLF 提示；
- Project-to-Act `--validate`：valid；
- Provider 调用：0；数据库迁移：0；默认路由切换：0。

## 关键哈希

- DTO：`37A361CD69BC928023D0C9E84618FD40A617BAB786F13B24EBF8B13FEBD4F56C`
- shadow service：`3E78A06B75189EC0363C3771B6B16515D03C19D107E707D0931C96748D0C01A9`
- evaluator：`F6A3C1277B90316FD3714F52D018B30FD8ACF38DD9FC56C6D20AD8808D86FD63`
- provider adapter：`EDD561608A0F1824B90453927DC75989D76CE6CCD898C03D0409A53597060C76`
- Prompt：`DE2D528EBDE6EC042CD50A809EC3CED898FC895BC075B2E11536F67145156BFA`
- dataset：`F52F13244EE47039D0F4B9BE438A70A234AC9FD32944B40E45808CACB8D5618E`

## 未通过与下一 Gate

- `review_status=draft_user_review_required`，所以即使黄金自测为 15/15，质量 Gate 仍强制 blocked；
- 未运行真实模型，不能报告真实 precision/recall、token、延迟或稳定性；
- 本地虚拟环境缺少 wheel build frontend，未额外生成 wheel 检查 package data；源码运行已由 Provider/Prompt 单元测试覆盖；
- 下一步由用户逐 case 审核 required/allowed/forbidden、引文、owner、epistemic 与 temporal 标注。批准并授权后才运行 M1 模型效果测试，之后再决定修 M1 或启动 M2。
