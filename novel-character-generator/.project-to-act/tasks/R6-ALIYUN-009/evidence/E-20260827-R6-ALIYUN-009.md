# E-20260827-R6-ALIYUN-009

- 实现 `DashScopeImageProvider`：官方异步创建任务、状态查询、结果下载。
- 模型范围固定为 `qwen-image-plus` / `qwen-image`；首轮默认 `qwen-image-plus`、`n=1`、`1328*1328`、`prompt_extend=false`、`watermark=false`。
- 正向/反向 Prompt 超过 800/500 字符时失败关闭；输出尺寸仅允许官方异步接口五档。
- API Key 只发往显式配置的 HTTPS `*.maas.aliyuncs.com` 业务空间域名；签名结果下载不携带 API Key，且仅允许 HTTPS `*.aliyuncs.com`。
- 明确 4xx 进入 submit rejected；网络/响应不确定进入 submission_unknown，禁止盲重提。
- PENDING/RUNNING 通过 `step.deferred` 释放 lease，10 秒后再查且不消耗错误重试。
- 生图 Worker 改为 Provider registry 单路径；Mock、DashScope 和后续市场模型统一构造/关闭。未知、重复或身份不匹配的适配器失败关闭，不回退 Mock。
- GeneratedImage 记录 Provider ID 与模型/适配器版本，避免跨模型 baseline 混算；插件契约见 `docs/26-image-provider-plugin-contract.md`。
- 同步 Provider 的 `artifact_refs` 与异步 Provider 的 `task_id` 都持久化到 ExternalOperation；重启后不依赖内存恢复。数据库 head 升为 `c4b8a7d219e1`，upgrade/downgrade/upgrade 验证退出 0。
- 2026-08-28：当前本地应用数据库已由 `f9a1e5c72d30` 升级到 `c4b8a7d219e1 (head)`；未调用图像 Provider、未产生费用。
- 新增单张 expected-field smoke 脚本；本机探测结果为 API Key 和业务空间 URL 均未配置，因此没有发起真实收费调用、没有图片产物。
- 验证：20 项 Provider/Registry/Settings/Image Pipeline 定向测试通过；211 项完整 Pytest 通过；Ruff 全项目通过；Mypy 119 个 source files 通过；Alembic 往返迁移和 smoke CLI help 通过。
- 非结论：真实图像效果、可信人工审批、漂移审计、质量 Gate 和 baseline 尚未验收。
