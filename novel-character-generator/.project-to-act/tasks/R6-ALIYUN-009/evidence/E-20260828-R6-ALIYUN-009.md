# E-20260828-R6-ALIYUN-009

- 时间：2026-08-28（Asia/Shanghai）
- 范围：版本化/可插拔 PromptRenderer、自然语言 Prompt provenance、DashScope 官方共享 URL 兼容和一张真实候选图，以及 timicc/gpt-image-2 的通用 OpenAI-compatible 同步适配。
- 代码状态：工作树含用户与其他任务的既有未提交修改；本证据只覆盖 R6-ALIYUN-009 允许路径，不声明仓库整体 clean。

## 实现证据

- 新增 `canonical-zh:canonical-zh-character-v1`，把 identity/stage/outfit/performance/environment/art-direction 字段确定性翻译为自然中文。
- 每个正向/负向语义短句绑定原始 `field_path` 与 `source_refs`；缺少结构化字段或来源时在收费提交前失败关闭。
- PromptRenderer 使用独立 registry；未知、重复和身份不一致失败关闭。Renderer ID/version 进入 `GeneratedImage.evaluation`。
- DashScope 接受官方共享域名及业务空间专属域名；规范化可选 `/api/v1`，避免形成 `/api/v1/api/v1`。
- Renderer SHA-256：`eb798625a6c0e547decf3dddf60dbc7d3fe1cb5f1cd15c63dee697a9007cdc63`。
- Golden SHA-256：`26d95ccf76383c8b1800c1ae43b724b4b0b32823081ad023aff1acf8b28879aa`。

## 自动验证

- `ruff check .`：退出 0；存在两个不可访问测试临时目录 warning，不影响扫描结论。
- 修改范围 Mypy：7 个源文件，退出 0。
- 完整 Pytest：225 collected，退出 0。
- Prompt 记录：`canonical-zh-character-v1`，24 clauses，其中 1 条纯工作流指令、23 条带来源绑定。
- 全仓 `mypy src` 曾暴露 `extraction_evaluation_service.py` 3 个既有类型错误；该文件不在本任务修改范围，未将其误记为本增量通过。随后只对 7 个修改源文件执行严格检查并通过。

## 真实 Provider 证据

- Key 与 URL 仅从本地 `.env` 读取，未写入证据、日志、测试或产物。
- 首次沙箱内尝试在 TCP 建连前失败，无远程任务 ID。
- 首次联网请求因旧实现重复拼接 `/api/v1` 得到确定性 HTTP 404，无远程任务 ID；修复后才再次提交。
- 成功任务 ID：`600fda07-1392-4d86-b005-7253a70b6aab`。
- Provider/model：`dashscope` / `qwen-image-plus`。
- seed：`270827`；尺寸：`1328x1328`；单候选、单图。
- ImageRenderSpec hash：`65bf1626e1d59480792eb6dad67e3ecf82067d0a23eba276f5de3168d49b411d`。
- PNG：`data/diagnostics/live-image-smoke/baseline-candidate-v1-20260828T001904Z.png`，1,698,466 bytes，SHA-256 `ecd6bfc2c87a08a1520c1006e3044dc2c52a54a4d01a4007751cd8408577f5d4`。
- Prompt/provenance：同名 `.prompt.json`，SHA-256 `6deaceedbd336ad78d11a4e35ffbc9c0d88b42b8b12f9f277bb9e89d20d083cb`。
- Provider 未返回费用，本证据不伪造成本数据。

## timicc / GPT Image 2 增量

- TIMI CC 官方公开文档确认 OpenAI-compatible Base URL 为 `https://timicc.com`；公开页未列当前 Key 分组的 image2 模型清单，故真实授权必须实调确认。
- 新增 `OpenAICompatibleImageProvider`；请求固定 `POST /v1/images/generations`、`model=gpt-image-2`、单图、PNG。现有 1328×1328 spec 转为官方 `1328x1328`，负向约束以自然视觉语言合入 Prompt。
- GPT Image 同步 base64 返回先解码、PNG/大小校验并按 SHA-256 原子写入 `data/provider-staging/timicc`，再返回 `staged-image://timicc/<sha256>.png`；重启后可下载并复核 hash，不把 base64 写入数据库。
- Base URL 只接受 HTTPS、`timicc.com` 和空路径或 `/v1`；API Key 只发往该 API host。本地暂存读取不发送凭据；Provider 错误只记录脱敏 code/request_id，不记录 message、Prompt 或 Key。
- 验证：23 项定向 Pytest、230 项完整 Pytest、全 `src tests scripts` Ruff、3 个修改源文件严格 Mypy 均退出 0。完整 Pytest 仅有一个第三方 Starlette/httpx 弃用 warning。
- 用户随后把 `TIMICC_API_KEY` 保存到项目 `.env`；检查只输出是否非空，未输出值。沙箱内首次 TCP 建连失败且没有远端响应；获准联网后只发起一次远端请求并成功，无自动收费重试。
- Provider/model：`timicc` / `gpt-image-2`；quality `medium`；耗时 32.364 秒；Provider 不支持 seed，sidecar 显式记录该事实。
- PNG：`data/diagnostics/live-image-smoke/baseline-candidate-gpt-image-2-v1-20260828T005527Z.png`，2,384,403 bytes，SHA-256 `b4ce6cb502a51fc62846bc270bf37fac0dae24bba7ace98129dbfcf9c578b236`。
- Prompt/provenance：同名 `.prompt.json`，24 clauses，SHA-256 `a6364b8ebc8edac6fa497247e79a8fa52c9742a0194b2a3b00ab37c899e00f9f`。
- 内容初检命中：五六岁儿童、黑色短发、小麦肤色、瘦小、灰色旧布衣、山石坐姿、望向东方晨光、山顶群山薄雾、全身、单人物；未见文字、水印、现代服装、成人体型或明显多余肢体。整体偏写实电影概念图。
- 漂移：请求 `1328x1328`，实际 PNG 为 `1024x1536`。产物保留为 `Baseline Candidate / waiting_user_approval`，但尺寸一致性 Gate 不通过；未自动再次付费生成。

## 视觉初检与 Gate

- 命中：儿童年龄感、黑色短发、小麦肤色、瘦小体型、灰色旧布衣、山石坐姿、东方晨光、群山薄雾、单人物；未见文字、水印或明显多余肢体。
- 待审：具体脸部由模型补全；风格偏柔和动画/3D，不足以自动证明“东方玄幻概念插画”达到用户最终审美。
- 数据库检查：`approved/locked` RenderProfile 数为 0。当前图片来自有界 expected-field fixture，不是正式获批快照。
- Gate：PromptRenderer/Provider/真实单图开发增量通过；图片状态为 `Baseline Candidate v1 / waiting_user_approval`。可信审批、DriftAudit、GateDecision、BaselineSelection 与批量生成仍未通过。
- 有效期：Renderer、golden、Provider endpoint/model、ImageRenderSpec、工作流默认值或图片 Gate 变化前。
